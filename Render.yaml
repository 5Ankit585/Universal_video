services:
  - type: web
    name: mediavault-pro
    runtime: python
    region: oregon

    buildCommand: "apt-get install -y ffmpeg && pip install -r requirements.txt"
    startCommand: "python app.py"

    envVars:
      - key: RENDER
        value: "true"
      - key: PORT
        value: "10000"